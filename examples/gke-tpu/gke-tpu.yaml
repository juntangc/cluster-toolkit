# Copyright 2026 "Google LLC"
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

blueprint_name: gke-tpu

vars:
  # The following variables should be over-written in the deployment.yaml file.
  # Your GCP Project ID
  project_id:

  # This should be unique across all of your Cluster Toolkit Deployments.
  deployment_name: gke-tpu

  # The GCP Region used for this deployment.
  region:

  # The GCP Zone used for this deployment.
  zone:

  # The number of TPU slices to create
  num_slices: 1

  # Machine type:
  # TPU v4:  ct4p-hightpu-4t
  # TPU v5e: ct5lp-hightpu-1t, ct5lp-hightpu-4t, ct5lp-hightpu-8t
  # TPU v5p: ct5p-hightpu-4t
  # TPU v6e: ct6e-standard-4t
  # TPU 7x:  tpu7x-standard-4t
  machine_type:

  # The TPU placement topology for pod slice node pool.
  # Examples:
  # TPU v4:  2x2x1, 2x2x2, 2x4x4, 4x4x4
  # TPU v5e: 2x2, 2x4, 4x4, 8x8
  # TPU v5p: 2x2x1, 2x2x2, 2x2x4, 2x4x4, 4x4x4
  # TPU v6e: 2x4, 4x4, 4x8, 8x8
  # TPU 7x:  2x2x1, 2x2x2
  tpu_topology:

  # Cidr block containing the IP of the machine calling terraform.
  # To allow all (IAM restrictions still enforced), use 0.0.0.0/0
  # To allow only your IP address, use <YOUR-IP-ADDRESS>/32
  authorized_cidr:

  # The name of the compute engine reservation of TPU nodes (optional)
  reservation: ""

  # Provision TPU nodes as Spot VMs (cannot be true when reservation is provided)
  spot: false

  # System and TPU node pool disk settings
  system_node_pool_disk_size_gb: 200
  node_pool_disk_size_gb: 100
  node_pool_disk_type: pd-balanced

  # Accelerator type for Kueue configuration
  # TPU v4:  tpu-v4-podslice
  # TPU v5e: tpu-v5-lite-podslice
  # TPU v5p: tpu-v5p-slice
  # TPU v6e: tpu-v6e-slice
  # TPU 7x:  tpu7x
  accelerator_type:

  # Enable Pathways for TPUs (provisions CPU node pool and configures Kueue quotas)
  enable_pathways_for_tpus: true

  # The namespace to be configured for running workloads
  user_namespace: default

deployment_groups:
- group: primary
  modules:
  - id: network
    source: modules/network/vpc
    settings:
      subnetworks:
      - subnet_name: $(vars.deployment_name)-sub-0
        subnet_region: $(vars.region)
        subnet_ip: 192.168.0.0/18
      secondary_ranges_list:
      - subnetwork_name: $(vars.deployment_name)-sub-0
        ranges:
        - range_name: pods
          ip_cidr_range: 10.4.0.0/14
        - range_name: services
          ip_cidr_range: 10.0.32.0/20
      firewall_rules:
      - name: $(vars.deployment_name)-internal-0
        ranges: [192.168.0.0/18]
        allow:
        - protocol: tcp
          ports: ["0-65535"]
        - protocol: udp
          ports: ["0-65535"]
        - protocol: icmp

  - id: node_pool_service_account
    source: modules/project/service-account
    settings:
      name: gke-np-sa
      project_roles:
      - logging.logWriter
      - monitoring.metricWriter
      - monitoring.viewer
      - stackdriver.resourceMetadata.writer
      - storage.objectViewer
      - artifactregistry.reader

  - id: workload_service_account
    source: modules/project/service-account
    settings:
      name: gke-wl-sa
      project_roles:
      - logging.logWriter
      - monitoring.metricWriter
      - monitoring.viewer
      - stackdriver.resourceMetadata.writer
      - storage.admin
      - artifactregistry.reader
      - container.admin
      - hypercomputecluster.editor

  - id: gke-tpu-cluster
    source: modules/scheduler/gke-cluster
    use: [network, workload_service_account]
    settings:
      system_node_pool_machine_type: "n2-standard-8"
      system_node_pool_disk_size_gb: $(vars.system_node_pool_disk_size_gb)
      system_node_pool_taints: []
      enable_private_endpoint: false # Allows access from authorized public IPs
      configure_workload_identity_sa: true
      namespace: $(vars.user_namespace)
      enable_ml_diagnostics: true
      enable_pathways_for_tpus: $(vars.enable_pathways_for_tpus)
      master_authorized_networks:
      - cidr_block: $(vars.authorized_cidr) # Allows your machine to run the kubectl command. Required for multi network setup.
        display_name: "kubectl-access-network"
      # Cluster versions cannot be updated through the toolkit after creation
      # Please manage cluster version from the Google Cloud Console directly
      version_prefix: "1.35."
      release_channel: REGULAR
      maintenance_exclusions:
      - name: no-minor-or-node-upgrades-indefinite
        exclusion_scope: NO_MINOR_OR_NODE_UPGRADES
        exclusion_end_time_behavior: UNTIL_END_OF_SUPPORT
    outputs: [instructions]

  - id: gke-tpu-pool
    source: modules/compute/gke-node-pool
    use: [gke-tpu-cluster, node_pool_service_account]
    settings:
      num_slices: $(vars.num_slices)
      name: $(vars.deployment_name)-pool
      disk_type: $(vars.node_pool_disk_type)
      machine_type: $(vars.machine_type)
      auto_upgrade: true
      zones: [$(vars.zone)]
      disk_size_gb: $(vars.node_pool_disk_size_gb)
      spot: $(vars.spot)
      reservation_affinity:
        consume_reservation_type: '$(vars.reservation != null && vars.reservation != "" ? "SPECIFIC_RESERVATION" : "NO_RESERVATION")'
        specific_reservations: '$(vars.reservation != null && vars.reservation != "" ? [{ name: vars.reservation }] : [])'
      placement_policy:
        type: COMPACT
        tpu_topology: $(vars.tpu_topology)
    outputs: [instructions]

  - id: workload-manager-install
    source: modules/management/kubectl-apply
    use: [gke-tpu-cluster]
    settings:
      kueue:
        install: true
        config_template_vars:
          tpu_quota: $(vars.num_slices * gke-tpu-pool.node_count_static * gke-tpu-pool.tpu_chips_per_node)
          accelerator_type: $(vars.accelerator_type)
          namespace: $(vars.user_namespace)
          tpu_topology: $(vars.tpu_topology)
      jobset:
        install: true
      cert_manager:
        install: true
